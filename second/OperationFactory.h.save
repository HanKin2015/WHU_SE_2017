#ifndef FRACTION_H_
#define FRACTION_H_
#include <iostream>
#include "Fraction.h"
using  namespace std;

//运算工厂类
class OperationFactory {

public:
    Fraction Calculation(string operate) {
        Fraction ans;
        switch(operate) {
        case "+":
            ans = 
            break;
        }
    }
protected:

private:

}
#endif;
